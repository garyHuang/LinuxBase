# rm 命令

## cp 复制文件
```
# 拷贝文件到当前目录
cp 1/1.txt .
# 拷贝文件并重命名
cp 1/1.txt a.txt
# 复制1目录的内容,当2文件夹不存在的时候，直接复制到当前目录下，并创建2目录，当2目录存在的时候，将1目录复制到2目录下
cp -r 1 2 
# 复制显示复制进度
cp -rv 1 2 

# 创建链接文件,两个文件的inode值一样,可以删除任意一个文件，剩下的文件可以继续使用不影响
cp -l 1.txt 2.txt
```