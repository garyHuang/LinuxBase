# sort 命令


## 1、使用
```
 sort -t' ' -k3 -n -r -u 1.txt
 -t 指定分隔符 -k指定排序的索引
 -r 倒序排序，没有-t正序排序
 -n 根据数字排序，字符串排序 10会在2的前面，加-n后2会在10的前面
 -u 指定去掉重复记录
 sort -un 1.txt -[x] 当去重复和数字排序组合在一起的时候，会将所有的字符串行只保留一行，其他的当作重复去掉
```

